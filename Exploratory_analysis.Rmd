---
title: "Exploratory data analysis"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse) # Основной пакет
library(readxl) # для чтения файлов экселя xls xlsx
library(skimr)  # для описательной статистики
library(writexl)  # для записи файлов экселя xls xlsx
library(corrr) # для составления корреляционной матрицы
library(pheatmap) # для постройки хитмэпа
library(flextable) # для красивых таблиц
library(ggExtra) # для соединения графиков
library(ggpubr) # для соединения графиков
library(GGally)
library(corrplot)


library(BlandAltmanLeh) # для Bland-Altman plots

```

## Load dataset
```{r}
vis01 <- read_excel("data/originals/Visit1.xls") %>% round(digits = 3)
vis02 <- read_excel("data/originals/Visit2.xls") %>% round(digits = 3)
vis03 <- read_excel("data/originals/Visit3.xls") %>% round(digits = 3)
vis04 <- read_excel("data/originals/Visit4.xls") %>% round(digits = 3)
vis05 <- read_excel("data/originals/Visit5.xls") %>% round(digits = 3)
vis06 <- read_excel("data/originals/Visit6.xls") %>% round(digits = 3)
vis07 <- read_excel("data/originals/Visit7.xls") %>% round(digits = 3)
vis08 <- read_excel("data/originals/Visit8(current).xls") 
ci_new <- read_excel("data/originals/CI_new.xls")
ci_fun <- read_excel("data/originals/cognitivefunctions.xls")


vis08 <- vis08 %>% 
  mutate(across(where(is.character), ~as.numeric(.))) 

```


## Bind datasets, save csv

```{r}
vis_list <- list(vis01, vis02, vis03, vis04, vis05, vis06, vis07, vis08, ci_new) # убрал ci_fun

vis_all <- reduce(vis_list, full_join, by = "id")

# vis_all <- vis_all %>% 
#   select(id, order(names(.)))

write_csv(vis_all, "data/raw/vis_all.csv" ) 

```

## Mutate in Factor

```{r}
#Определяем ранги степеней АГ
level_order <- c("n_level", "h_level", "AG1", "AG2", "AG3")
```

```{r}
vis_all <- vis_all %>%
  mutate(
    # Новая переменная data$cg_imp_all 1 -нет нарушений,  2 есть нарушения.
    cg_imp_all = case_when(
      cognitiveimpairment_cl %in% c(2, 3) | memoryloss_v8 == 1 ~ 2,
      cognitiveimpairment_cl == 1 ~ 1,
      .default = NA
    ),
    across(c(smoking_v1, smoking_v2, smoking_v3, smoking_v4, smoking_v5) , function(x)
      x %>% factor(
        levels = c(1, 2),
        labels = c("Current smoker", "Non-smoker")
      )),
    across(c(smoking_v6, smoking_v8) , function(x)
      x %>% factor(
        levels = c(1, 3, 2),
        labels = c("Current smoker", "Non-smoker", "Former smoker") # изменил  "Former smoker" на "Current smoker"
      )),
    across(c(smoking_v7) , function(x)
      x %>% factor(
        levels = c(1, 3, 2),
        labels = c("Current smoker", "Non-smoker", "Current smoker")
      )),
    across(c(alcohol_v5, alcohol_v6, alcohol_v8) , function(x)
      x %>% factor(levels = c(1, 2),
                   labels = c("Yes", "No"))),
    across(c(alcohol_v7) , function(x)
      x %>% factor(
        levels = c(1, 2, 3, 4, 5, 6, 7),
        labels = c("Yes", "Yes", "Yes", "Yes", "Yes", "No", "Yes")
      )),
    across(c(education_v7) , function(x)
      x %>% factor(
        levels = c(1, 2, 3, 4, 5),
        labels = c(
          "Completed higher education",
          "Incomplete higher education",
          "Secondary specialized education",
          "Secondary education",
          "Incomplete secondary education"
        )
      )),
    across(c(education_v8) , function(x)
      x %>% factor(
        levels = c(1, 2, 3, 4, 5, 6),
        labels = c(
          "Academic degree",
          "Completed higher education",
          "Incomplete higher education",
          "Secondary specialized education",
          "Secondary education",
          "Incomplete secondary education"
        )
      )),
    highbp_v7= if_else(highbp_v7 == 9,  NA,highbp_v7 ),
    across(c(highbp_v7) , function(x)
      x %>% factor(
        levels = c(1, 2),
        labels = c("Yes", "No")
      )),
     highbp_v8= if_else(highbp_v8 == 3,  NA,highbp_v8 ),
    across(c(highbp_v8) , function(x)
      x %>% factor(
        levels = c(1, 2),
        labels = c("Yes", "No")
      )),
    across(c(drugs_v7, drugs_v8) , function(x)
      x %>% factor(levels = c(1, 2),
                   labels = c("Yes", "No"))),
    across(c(drugs_v7, drugs_v8) , function(x)
      x %>% factor(levels = c(1, 2),
                   labels = c("Yes", "No"))),
    cad_v7= if_else(cad_v7 == 9,  NA,cad_v7 ),
     across(c(cad_v7) , function(x)
      x %>% factor(
        levels = c(1, 2),
        labels = c("Yes", "No")
      )),
     cad_v8= if_else(cad_v8 == 3,  NA,cad_v8 ),
    across(c(cad_v8) , function(x)
      x %>% factor(
        levels = c(1, 2),
        labels = c("Yes", "No")
      )),
    across(c(
      stroke_v7,
      braininjury_v7,
      diabetes_v7,
      renald_v7,
      pulmonaryd_v7,
      gastrod_v7,
      allergy_v7,
      inflammation_v7,
      fcad_v7,
      fah_v7,
      fstroke_v7,
      fdiabetes_v7,
      mcad_v7,
      mah_v7,
      mstroke_v7,
      mdiabetes_v7
    ) , function(x)
      x = ifelse(x ==9, NA, x)),
    
    across(c(
      stroke_v7,
      braininjury_v7,
      diabetes_v7,
      renald_v7,
      pulmonaryd_v7,
      gastrod_v7,
      allergy_v7,
      inflammation_v7,
      fcad_v7,
      fah_v7,
      fstroke_v7,
      fdiabetes_v7,
      mcad_v7,
      mah_v7,
      mstroke_v7,
      mdiabetes_v7
    ) , function(x)
      x %>% factor(
        levels = c(1, 2),
        labels = c("Yes", "No")
      )),
    across(c(
      stroke_v8,
      braininjury_v8,
      diabetes_v8,
      renald_v8,
      pulmonaryd_v8,
      gastrod_v8,
      allergy_v8,
      inflammation_v8,
      blood_v8,
      fcad_v8,
      fah_v8,
      fstroke_v8,
      fdiabetes_v8,
      mcad_v8,
      mah_v8,
      mstroke_v8,
      mdiabetes_v8
    ) , function(x)
      x = ifelse(x ==3, NA, x)),
    across(c(
      stroke_v8,
      braininjury_v8,
      diabetes_v8,
      renald_v8,
      pulmonaryd_v8,
      gastrod_v8,
      allergy_v8,
      inflammation_v8,
      blood_v8,
      fcad_v8,
      fah_v8,
      fstroke_v8,
      fdiabetes_v8,
      mcad_v8,
      mah_v8,
      mstroke_v8,
      mdiabetes_v8
    ) , function(x)
      x %>% factor(
        levels = c(1, 2),
        labels = c("Yes", "No")
      )),
    diabetestype_v7= if_else(diabetestype_v7 == 9,  NA,diabetestype_v7 ),
    across(c(diabetestype_v7) , function(x)
      x %>% factor(
        levels = c(1, 2),
        labels = c("Type I", "Type II")
      )),
    diabetestype_v8= if_else(diabetestype_v8 == 3,  NA,diabetestype_v8 ),
    across(c(diabetestype_v8) , function(x)
      x %>% factor(
        levels = c(1, 2),
        labels = c("Type I", "Type II")
      )),
    across(c(fliving_v7, fliving_v8, mliving_v7, mliving_v8) , function(x)
      x %>% factor(levels = c(1, 2),
                   labels = c("Alive", "Dead"))),
    across(c(fliving_v7, fliving_v8, mliving_v7, mliving_v8) , function(x)
      x %>% factor(levels = c(1, 2),
                   labels = c("Alive", "Dead"))),
    
    
    across(c(fcause_v7, fcause_v8, mcause_v7, mcause_v8) , function(x)
     x = ifelse(x ==6, NA, x)),
    across(c(fcause_v7, fcause_v8, mcause_v7, mcause_v8) , function(x)
      x %>% factor(
        levels = c(1, 2, 3 , 4, 5),
        labels = c(
          "Heart attack",
          "Stroke",
          "Cancer",
          "Chronic diseases",
          "Accident"
        )
      )),
    # Расчёт средних значений для замеров систолических и диастолических давлений
    sbp_all = rowMeans(select(
      ., c(sbp1_v8, sbp2_v8, sbp3_v8, smadsastotal_v8)
    ), na.rm = TRUE),
    dbp_all = rowMeans(select(
      ., c(dbp1_v8, dbp2_v8, dbp3_v8, smaddadtotal_v8)
    ), na.rm = TRUE),
   # Определение степени артериальной гипертензии
    AG_sbp = case_when(
      sbp_all < 130 ~ "n_level",
      sbp_all >= 130 & sbp_all < 140 ~ "h_level",
      sbp_all >= 140 & sbp_all < 160 ~ "AG1",
      sbp_all >= 160 & sbp_all < 180 ~ "AG2",
      sbp_all >= 180 ~ "AG3"
    ),
    AG_dbp = case_when(
      dbp_all < 85 ~ "n_level",
      dbp_all >= 85 & dbp_all < 90 ~ "h_level",
      dbp_all >= 90 & dbp_all < 100 ~ "AG1",
      dbp_all >= 100 & dbp_all < 110 ~ "AG2",
      dbp_all >= 110 ~ "AG3"
    ),
    #Выбираем худшую АГ из двух
    high_AG = case_when(
      is.na(AG_sbp) & !is.na(AG_dbp) ~ AG_dbp, 
      !is.na(AG_sbp) & is.na(AG_dbp) ~ AG_sbp, 
      !is.na(AG_sbp) & !is.na(AG_dbp) ~ {
        ifelse(
          match(AG_sbp, level_order) > match(AG_dbp, level_order),
          AG_sbp,
          AG_dbp
        )
      },
      TRUE ~ NA_character_) %>% factor( labels = c("n_level", "h_level", "AG1" , "AG2", "AG3"))
   )
    
    
vis_all$stroke_v7 %>% str()

write_rds(vis_all, "data/raw/vis_all.rds")
```


## выбор переменных
```{r}
# выбираем: id, курение, акоголь, инсульт, ЧМТ, диабет,заболевания почек в анамнезе , заболевания органов дыхания в анамнезе, заболевания ЖКТ, алергии, анемии,гнойные воспплаения, сведения о родителях, АД, масса тела,  липидограмма, когнитивные нарушения, наличие АГ


data <-
  vis_all %>% select(
    id,
    starts_with("smoking"),
    starts_with("alcoh"),
    starts_with("cad"),
    starts_with("stroke"),
    starts_with("braininjury"),
    starts_with("diabetes "),
    starts_with("renald"),
    starts_with("gastrod"),
    starts_with("allergy"),
    starts_with("blood"),
    starts_with("inflammation"),
    starts_with("covid"),
    starts_with("fliving"),
    starts_with("fdead"),
    starts_with("fcause"),
    starts_with("fcad"),
    starts_with("fah"),
    starts_with("fstroke"),
    starts_with("fdiabetes"),
    starts_with("mliving"),
    starts_with("mdead"),
    starts_with("mcause"),
    starts_with("mcad"),
    starts_with("mah"),
    starts_with("mstroke"),
    starts_with("mdiabetes"),
    starts_with("sbp"),
    starts_with("dbp"),
    starts_with("high_AG"),
    starts_with("weight"),
    starts_with("waist"),
    starts_with("hips"),
    starts_with("height"),
    starts_with("cholesterol"),
    starts_with("hdl"),
    starts_with("tg"),
    starts_with("ldl"),
    starts_with("vldl"),
    starts_with("atheroindex"),
    starts_with("glucose"),
    starts_with("cg_imp_all"),
     ) 

```



## Описательная статистика.
```{r}
a1 <-
  data %>% 
  select(is.factor) %>% 
  skim() %>% 
  select(skim_variable, n_missing, factor.n_unique, factor.top_counts) %>% 
  flextable() %>% 
  theme_box() %>%
  align(align = "center", part = "all")

a1 %>%  save_as_html( values = NULL, path ="data/raw/fltab_factor.html", lang = "en", title = "&#32;")


a2 <-
  data %>% 
  select(is.numeric) %>% 
  skim_without_charts() %>% 
  # select(skim_variable, n_missing, factor.n_unique, factor.top_counts) %>% 
  flextable() %>% 
  theme_box() %>%
  align(align = "center", part = "all")

a2 %>%  save_as_html( values = NULL, path ="data/raw/fltab_numeric.html", lang = "en", title = "&#32;")
```

##составление корреляционной матрицы
```{r, fig.width=12, fig.height=12}
data02 <- vis_all %>% select(
  id, tricepskinfold_v7, weight_v7, weight_v8, smoking_v8, alcohol_v8, cad_v8, sbp_all, dbp_all, cg_imp_all, height_v8, cholesterol_v8, hdl_v8, tg_v8, ldl_v8, vldl_v8
) 

data02 %>% select(is.numeric & -id) %>% na.omit() %>% cor() %>% corrplot(method = "number")
```

## Оценка согласованности шкал тестов (Тесты для оценки состояния когнитивной сферы)

```{r bland.altman.plot.cognitive, fig.height= 10, fig.width=20}
# Проверяем согласованость четырех шкал.
# 1. Краткая шкала оценки психического статуса (КШОПС) / Mini-mental State Examination (MMSE) – скрининговая переменная ‘mmse’
# 2.	Монреальская шкала когнитивной оценки (МоСА) – скрининговая, более чувствительная к когнитивным нарушениям сосудистой этиологии  переменная ‘moca’
# 3.	FAB (Frontal Assessment Batter - "Батарея лобной дисфункции") – выявляет нарушения лобных долей и их связи с подкорковыми структурами переменная ‘fab’
# 4.	Тест символьно-цифрового кодирования – оценка скорости мыслительных процессов и концентрации внимания (работа теменной и затылочной долей) переменная ‘symboldigit’

# Проверка будет осуществлятся следующими парами
# moca ~ mmse, moca ~ fab, moca ~ symboldigit,  mmse ~ fab, mmse ~ symboldigit,  fab ~ symboldigit


vis_all %>% select(starts_with(c("id", "moca_v8",  "mmse_v8", "fab_v8", "symboldigit_v8")))


MOCA_vs_MMSE <-  bland.altman.plot(
  vis_all$moca_v8,
  vis_all$mmse_v8,
  graph.sys = "ggplot2",
  conf.int = .95,
  geom_count = TRUE,
  silent = TRUE
) +
  labs (title = "MOCA vs MMSE",
        # subtitle = ,
        y = "Differences",
        x = "Means of measurements") +
  theme_bw() +
  theme (plot.title = element_text( 
                                    size = 30, 
                                    hjust = 0.5))

 

MOCA_vs_FAB <-  bland.altman.plot(
  vis_all$moca_v8,
  vis_all$fab_v8,
  graph.sys = "ggplot2",
  conf.int = .95,
  geom_count = TRUE,
  silent = TRUE
) +
  labs (title = "MOCA vs FAB",
        # subtitle = ,
        y = "Differences",
        x = "Means of measurements") +
  theme_bw() +
  theme (plot.title = element_text( 
                                    size = 30, 
                                    hjust = 0.5))


  
MOCA_vs_symboldigit <-  bland.altman.plot(
  vis_all$moca_v8,
  vis_all$symboldigit_v8,
  graph.sys = "ggplot2",
  conf.int = .95,
  geom_count = TRUE,
  silent = TRUE
) +
  labs (title = "MOCA vs symboldigit",
        # subtitle = ,
        y = "Differences",
        x = "Means of measurements") +
  theme_bw() +
  theme (plot.title = element_text( 
                                    size = 30, 
                                    hjust = 0.5))


MMSE_vs_FAB <-  bland.altman.plot(
  vis_all$mmse_v8,
  vis_all$fab_v8,
  graph.sys = "ggplot2",
  conf.int = .95,
  geom_count = TRUE,
  silent = TRUE
) +
  labs (title = "MMSE vs FAB",
        # subtitle = ,
        y = "Differences",
        x = "Means of measurements") +
  theme_bw() +
  theme (plot.title = element_text( 
                                    size = 30, 
                                    hjust = 0.5))

 

MMSE_vs_symboldigit <-  bland.altman.plot(
  vis_all$mmse_v8,
  vis_all$symboldigit_v8,
  graph.sys = "ggplot2",
  conf.int = .95,
  geom_count = TRUE,
  silent = TRUE
) +
  labs (title = "MMSE vs symboldigit",
        # subtitle = ,
        y = "Differences",
        x = "Means of measurements") +
  theme_bw() +
  theme (plot.title = element_text( 
                                    size = 30, 
                                    hjust = 0.5))



FAB_vs_symboldigit <-  bland.altman.plot(
  vis_all$fab_v8,
  vis_all$symboldigit_v8,
  graph.sys = "ggplot2",
  conf.int = .95,
  geom_count = TRUE,
  silent = TRUE
) +
  labs (title = "FAB vs symboldigit",
        # subtitle = ,
        y = "Differences",
        x = "Means of measurements") +
  theme_bw() +
  theme (plot.title = element_text( 
                                    size = 30, 
                                    hjust = 0.5))





p <- ggpubr::ggarrange(plotlist = list(MOCA_vs_MMSE, MOCA_vs_FAB, MOCA_vs_symboldigit, MMSE_vs_FAB , MMSE_vs_symboldigit, FAB_vs_symboldigit),
                  ncol = 3,
          nrow = 2,  
          heights = c(1,1),
          label.y = 0)

ggsave("bland_altman_plot_cognitive.png", plot = , path = "data/pics/", width = 20,
  height = 10)
```


## Оценка согласованности шкал тестов (Тесты для оценки эмоционально-аффективной сферы)

```{r bland.altman.plot.emotion, fig.height= 6, fig.width=20}
# Проверяем согласованость двух шкал.
# 1.	Шкала депрессии Бека-II  переменная ‘bdi’
# 2.	Шкала тревоги Cпилбергера в адаптации Ханина переменные ‘stsutiational’ и ‘stpersonal’
# 
#  Проверка будет осуществлятся следующими парами
#  bdi ~ stsutiational, bdi ~ stpersonal, stsutiational ~ stpersonal

vis_all %>% select(starts_with(c("id", "bdi_v8", "stsutiational_v8" , "stpersonal_v8")))

BDI_vs_stsutiational <-  bland.altman.plot(
  vis_all$bdi_v8,
  vis_all$stsutiational_v8,
  graph.sys = "ggplot2",
  conf.int = .95,
  geom_count = TRUE,
  silent = TRUE
) +
  labs (title = "BDI vs stsutiational",
        # subtitle = ,
        y = "Differences",
        x = "Means of measurements") +
  theme_bw() +
  theme (plot.title = element_text( 
                                    size = 30, 
                                    hjust = 0.5))

BDI_vs_stsutiational



BDI_vs_stpersonal <-  bland.altman.plot(
  vis_all$bdi_v8,
  vis_all$stpersonal_v8,
  graph.sys = "ggplot2",
  conf.int = .95,
  geom_count = TRUE,
  silent = TRUE
) +
  labs (title = "BDI vs stpersonal",
        # subtitle = ,
        y = "Differences",
        x = "Means of measurements") +
  theme_bw() +
  theme (plot.title = element_text( 
                                    size = 30, 
                                    hjust = 0.5))

BDI_vs_stpersonal



stsutiational_vs_stpersonal <-  bland.altman.plot(
  vis_all$bdi_v8,
  vis_all$stpersonal_v8,
  graph.sys = "ggplot2",
  conf.int = .95,
  geom_count = TRUE,
  silent = TRUE
) +
  labs (title = "stsutiational vs stpersonal",
        # subtitle = ,
        y = "Differences",
        x = "Means of measurements") +
  theme_bw() +
  theme (plot.title = element_text( 
                                    size = 30, 
                                    hjust = 0.5))


stsutiational_vs_stpersonal

p02 <- ggpubr::ggarrange(plotlist = list(BDI_vs_stpersonal, BDI_vs_stsutiational, stsutiational_vs_stpersonal),
                  ncol = 3,
          nrow = 1,  
          heights = c(1,1),
          label.y = 0)
p02

ggsave("bland_altman_plot_emotion.png", plot = p02, path = "data/pics/", width = 20,
  height = 6)

```




## Всякое
```{r}
# Проверка визитов
a1 <- vis_all %>% select(mmse_v8, moca_v8, fab_v8, stsutiational_v8, stpersonal_v8, symboldigit_v8, bdi_v8, mmse__cl, -moca_cl, cognitiveimpairment_cl, memoryloss_cl) %>%  summarise(across(everything(), ~ sum(!is.na(.))))



a1 %>% flextable() %>% 
  theme_box() %>%
  align(align = "center", part = "all") %>%  save_as_html( values = NULL, path ="data/raw/проверка визитов.html", lang = "en", title = "&#32;")

 vis_all %>% select(mmse_v8, moca_v8, fab_v8, stsutiational_v8, stpersonal_v8, symboldigit_v8, bdi_v8, mmse__cl, -moca_cl, cognitiveimpairment_cl,  memoryloss_cl) %>% na.omit() %>% summarise(across(everything(), ~ sum(!is.na(.))))%>% flextable() %>% 
  theme_box() %>%
  align(align = "center", part = "all") %>%  save_as_html( values = NULL, path ="data/raw/проверка визитов без пропусков.html", lang = "en", title = "&#32;")

 
 
```

